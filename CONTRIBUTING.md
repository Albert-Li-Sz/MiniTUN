# 贡献指南

感谢你参与 MiniTun。项目欢迎经过验证的缺陷修复、测试改进、文档完善、可维护性提升，
以及与项目边界一致的功能提案。

参与项目即表示你同意遵守[社区行为准则](CODE_OF_CONDUCT.md)，并同意所提交内容按
项目的 [MIT License](LICENSE) 授权。

## 开始之前

- 使用或部署问题请先阅读[文档索引](docs/README.md)和[故障排查](docs/troubleshooting.md)；
- 普通缺陷与功能建议可以提交 GitHub Issue；
- 较大功能、协议变化、数据库模式变化或公开 API 变化，应先通过 Issue 讨论设计；
- 安全漏洞不得提交公开 Issue，请遵循[安全策略](SECURITY.md)私下报告；
- 与 TCP 反向隧道无关，或要求 UDP、P2P、SOCKS5、FRP 兼容、多路复用的提案，
  当前不属于项目范围。

## 开发环境

完整依赖、构建预设、Sanitizer 和 fuzz 使用方法见[开发指南](docs/development.md)。
最基本的验证流程如下：

```bash
cmake --preset dev
cmake --build --preset dev
ctest --preset dev
```

提交前请确保构建目录由当前分支重新配置，且测试失败不是由陈旧生成文件造成。

## 推荐工作流

1. Fork 仓库并从最新 `main` 创建主题分支；
2. 将一次变更限制在一个清晰目标内，避免混入无关重构；
3. 先添加能够复现问题或描述预期行为的测试，再实现修改；
4. 根据改动风险运行对应测试矩阵；
5. 同步更新 README、技术文档、man 手册或变更日志；
6. 提交 Pull Request，并说明动机、设计、验证结果和兼容性影响。

建议使用简洁的分支名，例如 `fix/relay-timeout`、`feat/metrics` 或
`docs/install-guide`。项目不强制特定提交格式，但提交标题应使用祈使语气并准确描述
变更，例如 `fix: preserve reverse traffic after half-close`。

## 代码与设计要求

### C++ 与资源生命周期

- 使用 C++20，并遵循仓库现有命名、目录与头文件组织方式；
- 使用 RAII 表达资源所有权，避免裸所有权指针和隐式跨线程生命周期；
- 异步对象必须保持存活到相关回调或协程全部结束；
- strand 所有的状态只能在对应 strand 上修改；
- 错误应通过项目的稳定错误模型传播，不得向远程或 CLI 暴露异常实现细节；
- 不得使用无界队列、无界缓存或没有上限的外部输入分配；
- 新增超时、容量或计数配置时，必须提供安全默认值和明确上限。

### 协议、存储与兼容性

- 线协议字段必须显式编码，不能发送 C++ 结构体内存布局；
- 所有解析器都必须先校验长度、类型和状态，再分配或执行操作；
- 协议变化必须更新[远程协议](docs/protocol.md)、状态机测试和 fuzz 目标；
- 数据库模式变化必须使用事务化、可验证的版本迁移，不得静默重建用户数据库；
- 已公开的错误码、JSON 字段、退出码和软件包路径应保持兼容；如必须变更，应在提案
  和变更日志中明确说明。

### 安全与敏感数据

- Token、私钥和证书不得进入源码、测试夹具、日志、错误、命令参数或提交历史；
- 测试必须使用运行时生成或明显无效的临时凭据；
- 任何新的文件路径都必须考虑符号链接、硬链接、所有权、权限和 TOCTOU 风险；
- 使用 `--insecure-skip-verify` 的示例必须明确标注仅限开发，生产文档不得推荐该选项；
- 新增外部输入面时必须提供边界测试、失败隔离和日志脱敏。

### 依赖与平台

- 优先使用现有依赖和标准库，新增依赖必须说明必要性、许可证、维护状态和打包影响；
- FetchContent 依赖必须固定版本，发布构建必须继续支持发行版系统依赖；
- Linux 安装路径、systemd unit、sysusers 定义及 DEB/RPM 行为必须保持一致；
- 不得通过关闭编译器警告或 Sanitizer 来掩盖问题。

## 测试要求

| 改动类型 | 最低验证要求 |
| --- | --- |
| 文档 | 本地链接、命令示例、`git diff --check` |
| 公共工具或存储 | 相关单元测试和完整 CTest |
| IPC 或 CLI | IPC 单元测试、CLI/守护进程集成测试和完整 CTest |
| TLS、认证或协议 | 协议测试、TLS 集成测试、ASan/UBSan 和相关 fuzz 目标 |
| 并发、Worker 或关闭 | 完整 CTest、ASan/UBSan、TSan 和稳定性测试 |
| 中继数据路径 | TCP relay E2E、半关闭/背压测试及 Sanitizer |
| 安装或打包 | 安装布局测试、DEB/RPM 校验和干净容器冒烟测试 |
| GitHub Actions | actionlint、ShellCheck，并说明触发与权限变化 |

Pull Request 中应列出实际执行的命令及结果。无法运行某项平台测试时，请明确说明原因，
不要把未执行的检查描述为已通过。

## 文档要求

- 面向用户的行为变化必须同步更新 README、CLI、安装或故障排查文档；
- 架构、协议、安全边界或发布流程变化必须更新对应技术文档；
- 代码块中的命令、路径、字段名、状态值和退出码必须与实现一致；
- 文档使用简体中文，保留无法安全翻译的 API、命令、协议字段和软件包名称；
- 新增文档后应加入 [docs/README.md](docs/README.md) 和 README 文档导航。

## Pull Request 清单

提交前请确认：

- [ ] 变更目标单一，且没有覆盖他人的无关修改；
- [ ] 新行为有测试，缺陷修复包含回归用例；
- [ ] 必要的构建、测试、Sanitizer 或软件包检查已通过；
- [ ] 日志、错误和测试数据不包含敏感信息；
- [ ] 用户文档、技术文档和变更日志已同步；
- [ ] `git diff --check` 无错误；
- [ ] Pull Request 描述包含风险、兼容性和验证结果。

## 评审与合并

维护者会重点审查正确性、资源边界、并发生命周期、安全影响、向后兼容性、测试充分性
和运维可行性。评审可能要求拆分过大的变更或补充测试。获得评审并不免除提交者对代码
质量和授权来源的责任。

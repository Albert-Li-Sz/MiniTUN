# 安全策略

MiniTun 将安全边界、资源上限和失败隔离视为核心设计要求。本策略说明受支持版本、
漏洞报告流程、披露原则以及部署方责任。实现级控制与威胁边界详见
[安全设计](docs/security.md)。

## 受支持版本

| 版本 | 安全支持状态 |
| --- | --- |
| 当前 `main` 分支 | 支持，直至首个稳定 tag 发布 |
| `0.1.x` | 首个稳定 tag 发布后支持 |
| 更早的开发快照 | 不支持 |

MiniTun 0.1.0 已完成[阶段 16 最终验收](docs/acceptance.md)。验收证明所列测试与
控制已执行，但不构成绝对安全保证，也不替代部署方的风险评估。

## 私下报告漏洞

疑似安全漏洞不得提交公开 Issue、Pull Request 或讨论帖。请通过 GitHub 仓库的
**Security → Report a vulnerability** 功能创建私密 Security Advisory。

报告应尽可能包含：

- 受影响版本、提交 SHA、平台和安装方式；
- 漏洞类型、受影响组件和潜在影响；
- 最小复现步骤或概念验证；
- 攻击所需权限、网络位置和前置条件；
- 已知缓解措施或建议修复方向；
- 是否已经向其他人员或机构披露。

请使用临时、无效或专门为测试生成的凭据。切勿提交真实 Token、私钥、生产证书、
完整凭据数据库、未脱敏日志或第三方个人信息。

## 协调与披露

维护者会在可用时间内确认报告、评估影响并协调修复。项目目前不承诺固定响应或修复
SLA。报告者应在维护者完成评估、修复和发布前保持漏洞细节私密。

修复完成后，维护者可根据影响范围发布安全公告、受影响版本、缓解措施和升级建议。
公告会尽量感谢报告者，但需以报告者意愿和隐私要求为准。恶意利用、公开泄露未修复
细节或提交虚假报告不受本协调流程保护。

## 已实现的主要安全控制

- TLS 1.2 或更高版本，默认启用证书与主机名校验；
- HMAC-SHA256 质询认证、有界 nonce 重放保护、时钟偏差检查与认证限速；
- 每个服务端隔离的控制会话、会话代次、隧道注册表和 Worker Pool；
- 有界帧、字段、连接数、Worker 数、隧道数、等待时间和关闭期限；
- 受保护的 Unix 套接字、可信路径校验和精确文件权限；
- 状态与凭据分库，Token 不进入状态数据库、命令参数、响应或常规日志；
- 固定中继缓冲区、读写背压、TCP 半关闭和有期限取消；
- ASan、UBSan、TSan、libFuzzer 以及多服务器和软件包 E2E 验证。

## 已知边界

凭据数据库受文件权限保护，但未加密。以下主体或事件仍可能暴露凭据：

- root 或 `minitun` 服务账户；
- 具有同等权限的失陷进程；
- 特权内存检查、主机失陷或内核漏洞；
- 不安全的备份、快照、崩溃转储或日志采集系统；
- 管理员错误配置的目录权限、CA、证书、Token 或 systemd override。

MiniTun 不提供主机入侵防护、PKI 托管、防火墙管理、秘密轮换服务或磁盘加密。

## 部署方责任

生产部署方必须：

- 保护主机、文件系统、备份和管理账户；
- 使用可信 CA、正确的证书 SAN、私有密钥和高熵 Token；
- 对私钥与 Token 强制执行最小权限，并建立安全轮换流程；
- 将 `--allow-ports`、监听地址、客户端数、隧道数和连接数限制到业务所需范围；
- 禁止在生产环境使用 `--insecure-skip-verify`；
- 配置防火墙、监控、日志保留、时间同步和安全更新；
- 在升级前验证软件包来源与 SHA-256 清单，并备份持久化状态；
- 定期审查 `minitund.service`、`minitun-server.service` 及本地 override。

部署和运维建议见[安装指南](docs/installation.md)与[故障排查](docs/troubleshooting.md)。

# 支持说明

本文档说明 MiniTun 的支持渠道、问题报告要求和维护边界。

## 获取帮助前

请依次检查：

1. [README](README.md) 的快速开始与项目边界；
2. [文档索引](docs/README.md)中的相关用户或运维文档；
3. [故障排查](docs/troubleshooting.md)中的服务、TLS、IPC 与隧道诊断步骤；
4. 已有 GitHub Issues 中是否存在相同问题；
5. 是否可以在当前 `main` 或最新发布版本中复现。

## 支持渠道

- **可复现缺陷**：提交 GitHub Issue；
- **使用问题或文档疑问**：提交带清晰问题描述的 GitHub Issue；
- **功能建议**：先提交 Issue 说明使用场景、项目边界和替代方案；
- **安全漏洞**：不要提交公开 Issue，必须按[安全策略](SECURITY.md)私下报告；
- **贡献开发问题**：参阅[贡献指南](CONTRIBUTING.md)，并在相关 Issue 或 Pull
  Request 中讨论。

## 缺陷报告应包含

- MiniTun 版本、提交 SHA 和构建类型；
- Linux 发行版、版本、架构、内核和 systemd 版本；
- 安装方式：源码、DEB 或 RPM；
- `minitun version`、`minitund --version` 和 `minitun-server --version` 输出；
- 脱敏后的启动参数与相关配置；
- 最小复现步骤、预期结果和实际结果；
- 相关退出码、状态输出和最小必要日志；
- 问题是否稳定复现，以及最近一次正常工作的版本。

提交日志前必须移除 Token、私钥、证书私密内容、内网敏感地址、个人信息和其他凭据。
不要上传完整数据库；如确需描述数据库问题，请先提供模式版本、文件权限和脱敏错误。

## 支持范围

项目维护范围包括：

- 仓库中实现的 TCP 反向隧道功能；
- 文档列出的构建方式、CLI、协议和配置；
- 项目提供的 systemd、DEB 与 RPM 文件；
- 当前 CI 覆盖的编译器、依赖基线和 Linux 环境；
- 可复现的正确性、安全性、稳定性和兼容性缺陷。

以下事项通常不提供项目级保证：

- 第三方控制面、Web UI、FRP 配置或协议兼容；
- UDP、P2P、NAT 穿透、SOCKS5 或多路复用；
- 用户自定义代理、负载均衡器、防火墙、PKI 或容器平台的运维咨询；
- 已修改但未提供差异的私有分支或非官方二进制；
- 已停止支持的旧开发快照；
- 对响应时间、修复时间或特定部署可用性的商业 SLA。

## 版本与响应

稳定 tag 发布前，支持目标为当前 `main` 分支。稳定版本发布后，受支持版本会在
[安全策略](SECURITY.md)中更新。项目由维护者按可用时间提供社区支持，不承诺固定
响应时限。信息完整、复现清晰且附有测试的报告通常更容易得到处理。

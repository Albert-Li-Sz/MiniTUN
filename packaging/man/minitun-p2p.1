.TH MINITUN-P2P 1 "2026-08-12" "MiniTun 1.1.0" "用户命令"
.SH NAME
minitun-p2p \- 连接 MiniTun P2P tunnel，并自动回退到服务端 relay
.SH 用法
.B minitun-p2p
.I remote-endpoint
.RI [ options ]
.SH 描述
.B minitun-p2p
创建本地 TCP listener。每个连接先经 minitun-server 获取一次性认证的直连候选；
候选可达时选择 direct path，否则自动保留原连接作为 relay path。
.PP
当前实现适合 LAN 或具备可路由地址的主机，不包含 ICE、STUN、TURN 或 NAT 打洞。
direct path 使用一次性 token 认证，但不额外加密应用数据；敏感协议应自行启用 TLS。
.SH 参数
.TP
.I remote-endpoint
P2P tunnel 在 minitun-server 上的 host:port。
.SH 选项
.TP
.BI --listen " endpoint"
本地数值监听 endpoint；默认 127.0.0.1:6501。
.TP
.B --allow-non-loopback
允许把本地 connector 暴露到非 loopback 地址。
.TP
.B --relay-only
禁用 direct candidate，始终验证 relay fallback。
.TP
.BI --connect-timeout " seconds"
连接 minitun-server 的期限；默认 10 秒。
.TP
.BI --negotiation-timeout " seconds"
完成 direct/relay 选择的总期限；默认 5 秒。
.TP
.BI --direct-timeout " seconds"
直连候选的连接期限；默认 2 秒，且不得超过 negotiation timeout。
.TP
.BI --inactivity-timeout " seconds"
数据通道不活动超时；默认 300 秒。
.TP
.B --version
打印版本并退出。
.SH 另请参阅
.BR minitun (1),
.BR minitund (8),
.BR minitun-server (8)

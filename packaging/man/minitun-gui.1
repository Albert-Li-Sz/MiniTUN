.TH MINITUN-GUI 1 "2026-08-12" "MiniTun 1.1.0" "用户命令"
.SH NAME
minitun-gui \- MiniTun 本地 Web 管理控制台
.SH 用法
.B minitun-gui
.RI [ options ]
.SH 描述
.B minitun-gui
在数值 loopback 地址上提供静态 Web UI，并把同源 JSON API 请求转换成
.BR minitund (8)
本地 IPC 调用。它不直接读取数据库或凭据。
.PP
控制台可查看 server、tunnel、P2P 和诊断状态，并创建 TCP、UDP、SOCKS5 或 P2P
tunnel。写操作拒绝跨源请求，响应启用 CSP、frame、MIME 和 referrer 安全策略。
.SH 选项
.TP
.BI --listen " endpoint"
数值 loopback HTTP endpoint；默认 127.0.0.1:6500。禁止非 loopback 监听。
.TP
.BI --socket " path"
minitund Unix 控制 socket；默认 /run/minitun/minitun.sock。
.TP
.BI --assets-dir " directory"
构建后的 GUI 静态资源目录；软件包默认 /usr/share/minitun/gui。
.TP
.B --version
打印版本并退出。
.SH 安全
GUI 未提供公网认证层，故意只允许 loopback。需要远程管理时，应通过 SSH port
forward 等具备认证和加密的通道访问，不要把 listener 转发到不受信网络。
.SH 另请参阅
.BR minitun (1),
.BR minitund (8),
.BR ssh (1)

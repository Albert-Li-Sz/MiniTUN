MiniTun 不会安装或生成 TLS 私钥、客户端 PSK 或客户端策略。

启用 minitun-server.service 前，请由管理员安装：

  server.crt   PEM 证书链，minitun-server 必须具有读取权限
  server.key   匹配的 PEM 私钥，0600，owner minitun-server
  clients.json 严格客户端策略，0640，owner/group minitun-server
  client PSK   每客户端独立文件，0600，owner minitun-server

策略示例安装在 /etc/minitun-server/clients.json.example。若策略使用
certificate_san 或 certificate_sha256，还需通过 systemd override 增加 --client-ca。

软件包 unit 监听 0.0.0.0:2333。公开 tunnel 端口由 clients.json 的 allowed_ports
逐客户端限制；云防火墙也应只放行实际使用的端口。修改启动选项请使用
systemctl edit minitun-server，不要直接编辑发行方 unit。

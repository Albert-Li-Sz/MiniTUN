MiniTun 不会安装证书、私钥或认证 Token。

启用 minitun-server.service 前，请安装以下由管理员提供的文件：

  server.crt  PEM 证书链，minitun-server 必须具有读取权限
  server.key  匹配的 PEM 私钥，模式 0600，所有者为 minitun-server
  token       认证 Token，模式 0600，所有者为 minitun-server

软件包提供的 unit 监听 0.0.0.0:2333，不额外限制隧道端口，应用层默认允许全部有效
TCP 端口 1-65535。如需设置端口白名单或修改监听地址，请使用
systemctl edit minitun-server 创建 override，不要直接修改发行方 unit。

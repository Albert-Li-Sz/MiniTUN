#!/bin/sh
set -e

if [ "${1:-}" = purge ]; then
    rm -rf -- /var/lib/minitun-server /run/minitun-server
fi

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload >/dev/null 2>&1 || true
fi

exit 0
